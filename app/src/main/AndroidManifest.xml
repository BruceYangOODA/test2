<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.test2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".wirelessorder.PayActivity"></activity>
        <activity
            android:name=".wirelessorder.CheckTableActivity"
            android:theme="@android:style/Theme.Holo.Light.DarkActionBar" />
        <activity android:name=".wirelessorder.EditTableActivity" />
        <activity android:name=".wirelessorder.EditMenuActivity" />
        <activity
            android:name=".wirelessorder.QueryTableActivity"
            android:theme="@android:style/Theme.Holo.Light.DarkActionBar" />
        <activity
            android:name=".wirelessorder.QueryMenuActivity"
            android:theme="@android:style/Theme.Holo.Light.DarkActionBar" />
        <activity
            android:name=".wirelessorder.UpdateActivity"
            android:theme="@android:style/Theme.Holo.Light.DarkActionBar" />
        <activity android:name=".wirelessorder.OrderActivity" />

        <activity android:name=".wirelessorder.MainMenuActivity" />
        <activity android:name=".BoarderActivity" />
        <activity android:name=".YoutubeViewActivity" />
        <activity android:name=".WirelessOrderActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".wirelessorder.provider.TableProvider"
            android:authorities="com.amaker.provider.TABLES" />
    </application>

</manifest>